{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My Portfolio Project",
  "author": "gomezmark",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy:nuxt": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.0.0",
    "cross-env": "^6.0.3",
    "material-design-icons-iconfont": "^5.0.1",
    "push-dir": "^0.4.1"
  }
}
