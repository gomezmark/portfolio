{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "My Portfolio Project",
  "author": "gomezmark",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "generate:gh-pages": "cross-env DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy:nuxt": "push-dir --dir=lib --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.12.0",
    "@nuxtjs/vuetify": "^1.0.0",
    "cross-env": "^6.0.3",
    "material-design-icons-iconfont": "^5.0.1",
    "push-dir": "^0.4.1",
    "stylus": "^0.54.7",
    "stylus-loader": "^3.0.2"
  }
}
